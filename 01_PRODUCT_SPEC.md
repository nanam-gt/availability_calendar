# いちばん星ビレッジ 空き状況カレンダー Webアプリ仕様書

## 1. 目的
公式HPから閲覧できる宿泊施設の空き状況カレンダーを作成する。

現在の宿泊予約管理はExcelで行っているため、本アプリでは予約台帳との自動連携は行わない。スタッフが管理画面から公開用の空き状況を手動更新する。

将来的には有料サイトコントローラー導入を想定しているため、小規模・低コスト・保守しやすく、後から廃止・置換しやすい構成とする。

## 2. 前提
- 正式な予約管理はExcel
- 本アプリには宿泊者の個人情報を保存しない
- 一日一組限定の一棟貸切宿を想定
- 連泊あり
- 1日につき施設全体のステータスを1つ表示

## 3. 公開画面
- 1か月表示
- 現在月から3か月先まで閲覧可能
- 前月・翌月ボタン
- 現在月より前へは移動不可
- ステータスは3種類のみ
  - ○ 空き
  - △ キャンセル待ち
  - × 予約不可
- 未設定日は日付のみ表示
- 過去日は薄く表示しステータス非表示
- 日付をクリックしても何も起こらない

## 4. 注意書き
上部と下部に、空き状況がリアルタイムではないことを表示する。

例：
> ※表示している空き状況はリアルタイムではありません。予約申し込み後、スタッフが最新の予約状況を確認し、予約可否をご連絡いたします。

下部例：
> 空き表示の日程でも、ご予約状況によっては受付できない場合があります。お申し込み後、スタッフからのご案内をもって予約確定となります。

## 5. 予約申し込み
カレンダー下部に「宿泊予約を申し込む」ボタンを置く。

- リンク先はGoogleフォーム
- Google Forms APIは使わない
- Google OAuth等の認証連携はしない
- 単純な外部リンクとして実装
- URLは設定値 `RESERVATION_URL` で変更可能にする

## 6. 管理画面
URL例：
`/admin`

- 公開画面に管理画面リンクは表示しない
- 共通パスワード1つでログイン
- スタッフ別アカウントは作らない
- ログアウト機能あり
- ログイン状態は一定期間維持

## 7. 管理機能
### 1日変更
日付を選び、以下を設定：
- ○ 空き
- △ キャンセル待ち
- × 予約不可
- 未設定に戻す

### 複数日一括変更
スマホでも使いやすい複数選択モードを用意する。
Ctrl / Shiftキーを前提にしない。

## 8. 初期状態
未設定を基本とする。

未設定日は公開画面でステータスを表示しない。

## 9. データ
保存対象：
- 日付
- 公開ステータス
- 更新日時

保存禁止：
- 氏名
- 電話番号
- メールアドレス
- 住所
- 人数
- 料金
- 決済情報
- その他宿泊者情報

## 10. Excelとの関係
Excel = 正式な予約管理
Webカレンダー = お客様向け参考表示

自動連携しない。

## 11. 連泊
連泊の場合も、スタッフが予約を受けられない各日を手動で「予約不可」等に設定する。自動的なチェックイン・チェックアウト計算は行わない。

## 12. デザイン
既存のいちばん星ビレッジ公式HPの世界観に合わせる。

方向性：
- 自然
- 里山
- 温かみ
- 落ち着き
- 余白
- シンプル

スマートフォン最優先。

## 13. 将来
サイトコントローラー導入後は、公式HPのリンク先を変更するだけで本アプリを廃止できる構造にする。

## 14. 今回の確定条件
| 項目 | 決定 |
|---|---|
| 表示期間 | 3か月先まで |
| カレンダー | 1か月表示＋前月/翌月 |
| ステータス | ○空き / △キャンセル待ち / ×予約不可 |
| 日付クリック | 何も起こらない |
| 連泊 | あり |
| 管理認証 | 共通パスワード |
| 更新方法 | 1日変更＋複数日一括変更 |
| 初期状態 | 未設定 |
| 注意書き | 上部＋下部 |
| デザイン | 既存HPに合わせる |
